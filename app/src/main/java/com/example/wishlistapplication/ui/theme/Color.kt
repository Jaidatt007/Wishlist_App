package com.example.wishlistapplication.ui.theme

import androidx.compose.ui.graphics.Color


// Light Theme
val primaryColor_light = Color(0xFFA294F9) // Soft lavender for a light, calming feel
val onPrimaryColor_light = Color(0xFFFFFFFF) // White for contrast

val secondaryColor_light = Color(0xFFCDC1FF) // Gentle lilac for secondary accents
val onSecondaryColor_light = Color(0xFFFFFFFF) // White for contrast

val tertiaryColor_light = Color(0xFFE5D9F2) // Light pastel purple for tertiary elements
val onTertiaryColor_light = Color(0xFF000000) // Black for high contrast

val backgroundColor_light = Color(0xFFF5EFFF) // Soft lavender-white for a clean background
val onBackgroundColor_light = Color(0xFF000000) // Black for readability

val surfaceColor_light = Color(0xFFFFFFFF) // Pure white for surfaces
val onSurfaceColor_light = Color(0xFF000000) // Black for text on surfaces

val surfaceVariantColor_light = Color(0xFFE5D9F2) // Pastel purple for surface variants
val onSurfaceVariantColor_light = Color(0xFF000000) // Black for contrast

val errorColor_light = Color(0xFFBA1A1A) // Bold red for error states
val onErrorColor_light = Color(0xFFFFFFFF) // White for visibility

val disabledTextColor_light = Color(0xFF9E9E9E) // Muted gray for disabled text
val disabledBackgroundColor_light = Color(0xFFE5D9F2).copy(alpha = 0.3f) // Semi-transparent pastel purple
val disabledBorderColor_light = Color(0xFFCDC1FF).copy(alpha = 0.5f) // Semi-transparent lilac


// Warm theme
val primaryColor_warm = Color(0xFFD2691E) // Warm orange-brown, like chocolate or sienna
val onPrimaryColor_warm = Color(0xFFFFFFFF) // White for contrast

val secondaryColor_warm = Color(0xFFFFA07A) // Light Salmon for warm secondary accent
val onSecondaryColor_warm = Color(0xFF000000) // Black for contrast

val tertiaryColor_warm = Color(0xFFFFE4B5) // Moccasin for a warm, muted tertiary
val onTertiaryColor_warm = Color(0xFF000000) // Black for readability

val backgroundColor_warm = Color(0xFF2E1503) // Deep brown for a warm, inviting background
val onBackgroundColor_warm = Color(0xFFFFFFFF) // White for visibility

val surfaceColor_warm = Color(0xFF3E2723) // Rich warm brown for surfaces
val onSurfaceColor_warm = Color(0xFFFFFFFF) // White for text on surfaces

val surfaceVariantColor_warm = Color(0xFF5D4037) // Cocoa brown for surface variants
val onSurfaceVariantColor_warm = Color(0xFFFFFFFF) // White for contrast

val errorColor_warm = Color(0xFFB71C1C) // Deep red for error indication
val onErrorColor_warm = Color(0xFFFFFFFF) // White for visibility




// Dark Theme
val primaryColor_dark = Color(0xFF6750A4) // Deep purple, rich and dark
val onPrimaryColor_dark = Color(0xFFFFFFFF) // White for contrast

val secondaryColor_dark = Color(0xFF4E378B) // Darker violet for secondary elements
val onSecondaryColor_dark = Color(0xFFFFFFFF) // White for contrast

val tertiaryColor_dark = Color(0xFF311E53) // Dark, muted purple for tertiary elements
val onTertiaryColor_dark = Color(0xFFFFFFFF) // White for readability

val backgroundColor_dark = Color(0xFF1C1B1F) // Near-black background for a classic dark theme
val onBackgroundColor_dark = Color(0xFFFFFFFF) // White for text readability

val surfaceColor_dark = Color(0xFF25242A) // Dark gray for surfaces
val onSurfaceColor_dark = Color(0xFFFFFFFF) // White text on surfaces

val surfaceVariantColor_dark = Color(0xFF3B3844) // Muted gray-purple for surface variants
val onSurfaceVariantColor_dark = Color(0xFFFFFFFF) // White for contrast

val errorColor_dark = Color(0xFFCF6679) // Soft pink-red for error states
val onErrorColor_dark = Color(0xFF1C1B1F) // Dark text for error elements

val disabledBorderColor_dark = Color(0xFF6750A4).copy(alpha = 0.5f) // Semi-transparent primary color for borders

